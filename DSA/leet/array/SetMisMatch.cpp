// You have a set of integers s, which originally contains all the numbers from 1 to n. Unfortunately, due to some error, one of the numbers in s got duplicated to another number in the set, which results in repetition of one number and loss of another number.
// You are given an integer array nums representing the data status of this set after the error.
// Find the number that occurs twice and the number that is missing and return them in the form of an array.

// Bạn có một tập hợp số nguyên s, ban đầu chứa tất cả các số từ 1 đến n.

// Không may, do một lỗi nào đó:

// một số trong tập bị lặp lại (xuất hiện 2 lần),

// và vì thế một số khác bị mất (không còn xuất hiện).

// Bạn được cho một mảng số nguyên nums, biểu diễn trạng thái của tập hợp sau khi xảy ra lỗi.

// 👉 Hãy tìm:

// số bị lặp lại (xuất hiện hai lần),

// và số bị thiếu (không xuất hiện),

// rồi trả về chúng dưới dạng một mảng.