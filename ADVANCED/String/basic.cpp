#include<bits/stdc++.h>

using namespace std;

int main()
{
    int x; cin>>x; //khi nhập x thường sẽ bị trôi lệnh
    cin.ignore(); // xử lí trôi lệnh
    string s;
    getline(cin,s); // string có dấu cách
    //s.size(),s.leghth() : độ dài chuỗi
    //s.insert(<pos>,s1) : chèn s1 vào pos
    //s.erase(<pos>) : xóa mọi kí tự từ chỉ số pos
    //
}